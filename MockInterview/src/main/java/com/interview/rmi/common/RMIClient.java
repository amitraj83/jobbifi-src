package com.interview.rmi.common;



public class RMIClient {

  // private ReceiveMessageInterface rmiServer;
  // public RMIClient(String serverAddress, String serverPort, String rmiServerName){
  //
  // Registry registry;
  // try{
  // registry=LocateRegistry.getRegistry
  // (serverAddress,(new Integer(serverPort)).intValue());
  // rmiServer=(ReceiveMessageInterface)(registry.lookup(rmiServerName));
  //
  // }
  // catch(RemoteException e){
  // e.printStackTrace();
  // }
  // catch(NotBoundException e){
  // System.err.println(e);
  // }
  // }
  /*
   * public String executeRequest(String req, String type){ try { return
   * rmiServer.executeRequest(req, type); } catch (RemoteException e) { e.printStackTrace(); }
   * return ""; }
   */
}
